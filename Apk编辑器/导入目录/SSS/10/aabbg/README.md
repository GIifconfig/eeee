# Android multi-sensor & camera app

This project contains UI layout, AndroidManifest and Java source stubs extracted from user-provided content. Handlers are stubs/basic implementations; you should test and enhance them on a real device.
